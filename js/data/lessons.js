/* js/data/lessons.js — Контент уроків (розширений) */
/* Відео: українські YouTube-канали (Школа програмування, Викладач, SimpleCode UA) */

window.LESSONS_DATA = [
    {
        id: 'intro',
        title: 'Вступ до C++',
        level: 'beginner',
        xp: 10,
        levelLabel: 'Початківець',
        icon: '🚀',
        videos: [{ "id": "Lo1UKhw52ig", "title": "Уроки C++ для початківців / #1 – Введення в мову C++ з нуля ..." }, { "id": "1ekpbC1E6TY", "title": "Вступ до мови програмування C++" }],
        explanation: `<h3>1. Що таке C++?</h3>
<p><strong>C++</strong> — це потужна, швидка, багатоцільова мова програмування. Вона була створена Б'єрном Страуструпом у 1979 році як "C з класами" в Bell Labs, щоб поєднати низькорівневу ефективність мови C з високорівневими абстракціями ООП.</p>
<h4>Головні особливості C++:</h4>
<ul>
  <li>⚡ <strong>Висока продуктивність:</strong> C++ є компільованою мовою, тобто вихідний код перетворюється безпосередньо у машинний код (на відміну від інтерпретованих мов, таких як Python або JavaScript). Це робить програми на C++ надзвичайно швидкими.</li>
  <li>🎮 <strong>Майстерність у GameDev:</strong> Unreal Engine, один з найпопулярніших ігрових рушіїв, повністю написаний на C++ і вимагає його для розробки складних ігор (від The Witcher до Fortnite).</li>
  <li>🧠 <strong>Ручне управління пам'яттю:</strong> C++ дозволяє розробнику точно контролювати, коли і як виділяється та звільняється пам'ять, що критично для систем реального часу.</li>
  <li>🏛️ <strong>Багатопарадигмальність:</strong> Підтримує процедурне, об'єктно-орієнтоване та узагальнене (через шаблони) програмування.</li>
</ul>

<h3>2. Куди компілюється код?</h3>
<p>Шлях вашої програми від тексту до виконання складається з 3 етапів:</p>
<ol>
  <li><strong>Препроцесор:</strong> Обробляє директиви (рядки, що починаються з <code>#</code>). Наприклад, <code>#include &lt;iostream&gt;</code> копіює весь код бібліотеки <code>iostream</code> у ваш файл.</li>
  <li><strong>Компіляція:</strong> Текст перетворюється на об'єктний код (зрозумілий процесору).</li>
  <li><strong>Лінкування (Компонування):</strong> Збирає ваш об'єктний код та бібліотеки в один єдиний виконуваний файл (<code>.exe</code> на Windows, або просто бінарник на Linux).</li>
</ol>

<h3>3. Структура першої програми</h3>
<p>Для написання найпростішої програми нам потрібна бібліотека для роботи з консоллю та функція <code>main()</code>.</p>
<div class="note-box">
  💡 <strong>Важливо:</strong> Коли операційна система запускає вашу програму, вона <strong>завжди</strong> шукає функцію з назвою <code>main</code>. Без неї програма не скомпілюється.
</div>
<h4>Розбір коду:</h4>
<ul>
  <li><code>#include &lt;iostream&gt;</code> — Підключає бібліотеку вводу/виводу. "i" - input, "o" - output, "stream" - потік даних.</li>
  <li><code>using namespace std;</code> — Усі стандартні об'єкти (як <code>cout</code>) живуть у "квартирі" під назвою <code>std</code> (standard). Цей рядок каже компілятору: "Якщо ти не знаєш звідки взяти <code>cout</code>, шукай в <code>std</code>". Без цього довелося б писати <code>std::cout</code>.</li>
  <li><code>int main()</code> — Початок програми. <code>int</code> означає, що функція поверне ціле число.</li>
  <li><code>cout &lt;&lt; "Привіт!";</code> — Означає "виведи в консоль" (Character OUTput). Стрілочки <code>&lt;&lt;</code> показують напрямок руху даних (у консоль).</li>
  <li><code>endl</code> — End Line. Робить перехід на новий рядок. Аналог натисканню Enter.</li>
  <li><code>return 0;</code> — Повертає нуль операційній системі. 0 означає, що програма завершилась успішно без помилок.</li>
</ul>`,
        code: `#include <iostream>
using namespace std;

int main() {
    cout << "Привіт, Світ!" << endl;
    
    // cout можна "ланцюгувати"
    cout << "Вивчати C++ " << "це " << "круто!" << endl;
    return 0;
}`,
        codeExplanation: ["`#include <iostream>` — підключає бібліотеку вводу/виводу.", "`using namespace std;` — дозволяє не писати `std::`.", "`int main()` — головна функція програми.", "`cout <<` — направляє текст у потік виводу (на екран).", "`return 0;` — повідомляє ОС, що програма завершилась успішно."],
        practiceTask: 'Напишіть програму, яка виводить ваше ім\'я в першому рядку, та ваше місто у другому.',
        expectedOutput: 'Іван\nКиїв',
        hint: 'Використайте два оператори cout з endl, або один з декількома << та endl посередині.',
    },

    {
        id: 'variables',
        title: 'Змінні та типи даних',
        level: 'beginner',
        xp: 10,
        levelLabel: 'Початківець',
        icon: '📦',
        videos: [{ "id": "5MgT9H-y1ZU", "title": "Уроки C++ для початківців / #3 – Змінні та типи даних" }, { "id": "mnJN8Rtt5A4", "title": "#3 Примітивні типи даних" }],
        explanation: `<h3>1. Що таке змінна?</h3>
<p>Уявіть, що пам'ять комп'ютера — це величезна шафа з мільйонами пустих коробок. <strong>Змінна</strong> — це коли ми беремо одну коробку, клеїмо на неї наліпку з назвою (ім'я змінної) і визначаємо розмір цієї коробки (тип даних), щоб покласти туди певне значення (дані).</p>
<p>В C++ ви <strong>зобов'язані</strong> вказати тип даних перед створенням змінної (строга типізація). Це дозволяє компілятору точно знати, скільки пам'яті виділити.</p>

<h3>2. Базові типи даних</h3>
<table style="width:100%; text-align:left; border-collapse:collapse; margin-bottom:15px;">
  <tr style="border-bottom: 2px solid #555;">
    <th>Тип</th><th>Призначення</th><th>Розмір</th><th>Приклад</th>
  </tr>
  <tr><td><code>int</code></td><td>Цілі числа (від -2 млрд до +2 млрд)</td><td>4 байти</td><td><code>int age = 25;</code></td></tr>
  <tr><td><code>double</code></td><td>Дробові числа (з подвійною точністю)</td><td>8 байт</td><td><code>double pi = 3.14159;</code></td></tr>
  <tr><td><code>float</code></td><td>Дробові числа (з одинарною точністю)</td><td>4 байти</td><td><code>float price = 9.99f;</code></td></tr>
  <tr><td><code>char</code></td><td>Один символ (в одинарних лапках)</td><td>1 байт</td><td><code>char grade = 'A';</code></td></tr>
  <tr><td><code>bool</code></td><td>Логічний тип (істина або хиба)</td><td>1 байт</td><td><code>bool isStudent = true;</code></td></tr>
  <tr><td><code>string</code></td><td>Рядок тексту (потребує <code>#include &lt;string&gt;</code>)</td><td>Динамічний</td><td><code>string name = "John";</code></td></tr>
</table>

<h3>3. Іменування змінних</h3>
<p>В C++ є правила та найкращі практики іменування (Best Practices):</p>
<ul>
  <li>🟢 Можна використовувати букви, цифри та нижнє підкреслення <code>_</code>.</li>
  <li>🔴 <strong>НЕ</strong> можна починати з цифри (<code>1player</code> — помилка).</li>
  <li>🔴 <strong>НЕ</strong> можна використовувати пробіли або спецсимволи (<code>my age</code>, <code>my-age</code> — помилки).</li>
  <li>⭐ Найпопулярніший стиль в C++ — <strong>camelCase</strong> (наприклад: <code>playerScore</code>, <code>maxHealth</code>, <code>isGameOver</code>).</li>
</ul>

<h3>4. Константи (const)</h3>
<p>Якщо ви знаєте, що значення ніколи не зміниться протягом роботи програми, завжди робіть змінну константою за допомогою ключового слова <code>const</code>. Це убезпечить код від випадкових переписувань та може пришвидшити роботу програми.</p>
<code>const double PI = 3.14159;</code>
<p>Якщо спробувати зробити <code>PI = 3;</code>, компілятор видасть помилку.</p>
`,
        code: `#include <iostream>
#include <string>  // Для типу string
using namespace std;

int main() {
    // Оголошення та ініціалізація
    int age = 20;
    double height = 1.75;
    char grade = 'A';
    bool isStudent = true; // true(1) або false(0)
    string name = "Олександр";
    
    // Зміна значення змінної
    age = 21; // Тепер вік 21

    const int DAYS_IN_WEEK = 7; // Константа, змінити не можна

    // Вивід змінних
    cout << "Ім'я: " << name << endl;
    cout << "Вік: " << age << endl;
    cout << "Зріст: " << height << " м" << endl;
    cout << "Студент? " << isStudent << " (1 означає так)" << endl;

    return 0;
}`,
        codeExplanation: ["Кожна змінна оголошується як: `тип назва = значення;`", "`int` зберігає цілі числа, `double` — дробові.", "`string` зберігає текст у подвійних лапках, а `char` один символ в одинарних `''`.", "`const` робить змінну незмінною.", "Змінним можна задавати нове значення просто написавши `age = 21;` (вже без вказування типу)."],
        practiceTask: 'Створіть змінні: ім\'я (string), вік (int), бал (double). Виведіть їх кожну з нового рядка.',
        expectedOutput: 'Марія\n19\n4.75',
        hint: 'Не забудьте #include <string> на початку.',
    },

    {
        id: 'input_output',
        title: 'Ввід та вивід',
        level: 'beginner',
        xp: 10,
        levelLabel: 'Початківець',
        icon: '⌨️',
        videos: [{ "id": "ZY1yzZWYaC4", "title": "Програмування на C++ (2.5). cin/cout - ввід та вивід" }],
        explanation: `<h3>1. Що таке потоки (Streams)?</h3>
<p>В C++ ввід та вивід працює через <strong>потоки</strong> символів. 
Уявіть конвеєр: <code>cout</code> (Character Out) бере дані з вашої програми і виштовхує їх на екран (за допомогою оператора <code>&lt;&lt;</code>). 
А <code>cin</code> (Character In) — це приймальник. Він чекає, поки користувач введе щось на клавіатурі, і оператор <code>&gt;&gt;</code> "затягує" ці дані у підготовлену змінну.</p>

<h3>2. Зчитування даних (cin)</h3>
<p>Щоб щось зчитати, треба спочатку створити пусту коробку (змінну), куди <code>cin</code> покладе дані.</p>
<code>
int age;<br>
cin >> age; // Програма зупиниться і чекатиме введення числа
</code>
<div class="note-box">
  ⚠️ <strong>Особливість cin:</strong> <code>cin</code> працює до першого пробілу, табуляції або Enter-у. Тобто, якщо ви спробуєте зчитати ім'я та прізвище "Іван Франко" через <code>cin &gt;&gt; name;</code>, у змінну <code>name</code> потрапить ТІЛЬКИ "Іван" (те, що до пробілу).
</div>

<h3>3. Зчитування цілого рядка з пробілами (getline)</h3>
<p>Щоб зчитати цілий рядок тексту, включно з пробілами, замість <code>cin &gt;&gt;</code> використовують функцію <code>getline()</code>.</p>
<code>
string fullName;<br>
getline(cin, fullName); // Зчитає "Іван Франко" повністю
</code>

<h3>4. Естетика виведення (Маніпулятори)</h3>
<p>Іноді треба вивести дробове число строго з 2 знаками після коми (наприклад, ціну). Для цього використовують бібліотеку <code>&lt;iomanip&gt;</code>.</p>
<ul>
  <li><code>fixed</code> — каже виводити число не в науковому форматі, а звичайною десятковим дробом.</li>
  <li><code>setprecision(n)</code> — вказує скільки цифр після коми залишити (де n — число).</li>
</ul>`,
        code: `#include <iostream>
#include <string>
#include <iomanip> // Для setprecision
using namespace std;

int main() {
    string name;
    int age;
    double money;

    // Зчитуємо текст із пробілами
    cout << "Введіть ваше ім'я та прізвище: ";
    getline(cin, name);

    // Зчитуємо декілька змінних підряд
    cout << "Введіть вік та баланс через пробіл: ";
    cin >> age >> money;

    cout << "----------------------" << endl;
    cout << "Користувач: " << name << endl;
    cout << "Вік: " << age << " років" << endl;
    
    // Вивід балансу: fixed + setprecision(2) для 2 знаків після коми
    cout << "Баланс: $" << fixed << setprecision(2) << money << endl;

    return 0;
}`,
        codeExplanation: ["`getline(cin, name)` зчитує весь рядок тексту з пробілами.", "`cin >> age >> money` дозволяє зчитати декілька змінних підряд. Користувач може вводити їх через пробіл або Enter.", "`setprecision(2)` (потребує `<iomanip>`) обрізає вивід дробового числа до 2 знаків після коми (кругло заокруглюючи)."],
        practiceTask: 'Напишіть програму, що запитує два ЦІЛИХ числа через пробіл і виводить їхню суму.',
        expectedOutput: '15',
        hint: 'Створіть змінні int a, b. Зчитайте cin >> a >> b. Виведіть cout << a + b;',
    },

    {
        id: 'conditions',
        title: 'Умовні оператори (if-else)',
        level: 'beginner',
        xp: 15,
        levelLabel: 'Початківець',
        icon: '🔀',
        videos: [{ "id": "TSj_CSb24fw", "title": "Уроки C++ для початківців / #4 - Умовні конструкції." }],
        explanation: `<h3>1. Розгалуження алгоритму</h3>
<p>В реальному світі ми постійно приймаємо рішення: <em>"Якщо йде дощ, я візьму парасолю. Інакше – одягну бейсболку"</em>. У програмуванні для цього використовується умовна конструкція <code>if / else</code>.</p>

<h3>2. Синтаксис if-else</h3>
<p>Блок <code>if</code> завжди очікує <strong>умову</strong> у круглих дужках <code>()</code>. Ця умова має видавати <code>true</code> (істина) або <code>false</code> (хиба). Якщо умова істинна, виконується код всередині фігурних дужок <code>{}</code>.</p>
<code>
if (умова) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Виконається, якщо умова true<br>
} else if (інша_умова) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Виконається, якщо перша умова false, а ця - true<br>
} else {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Виконається, якщо жодна умова не спрацювала<br>
}
</code>

<h3>3. Оператори порівняння та логіки</h3>
<p>Щоб побудувати умову, використовуються компаратори:</p>
<table style="width:100%; text-align:left; border-collapse:collapse; margin-bottom:15px;">
  <tr style="border-bottom: 2px solid #555;">
    <th>Оператор</th><th>Що робить</th><th>Оператор</th><th>Що робить</th>
  </tr>
  <tr><td><code>==</code></td><td>Рівно (перевірка)</td><td><code>&&</code></td><td>Логічне <strong>ТА</strong> (AND)</td></tr>
  <tr><td><code>!=</code></td><td>Не рівно</td><td><code>||</code></td><td>Логічне <strong>АБО</strong> (OR)</td></tr>
  <tr><td><code>&gt; / &gt;=</code></td><td>Більше / Більше-рівно</td><td><code>!</code></td><td>Логічне <strong>НЕ</strong> (NOT)</td></tr>
</ul>
<div class="note-box">
  🔥 <strong>НАЙБІЛЬША ПОМИЛКА: плутати <code>=</code> та <code>==</code></strong><br>
  <code>a = 5</code> (Присвоєння: "Поклади 5 в коробку a").<br>
  <code>a == 5</code> (Перевірка: "То що там у коробці a, число 5?").<br>
  Якщо ви напишете <code>if (a = 5)</code>, програма мовчки присвоїть 5 і завжди буде вважати умову \`true\`.
</div>

<h3>4. Тернарний оператор (Скорочений if)</h3>
<p>Іноді <code>if-else</code> буває занадто громіздким для простих дій, наприклад присвоєння змінній. Для цього є <strong>тернарний оператор <code>? :</code></strong>.</p>
<code>string result = (score >= 60) ? "Здав!" : "Не здав";</code><br>
<p>Це читається як: "Якщо умова (score>=60) істинна (?), поверни 'Здав!', інакше (:) поверни 'Не здав'".</p>`,
        code: `#include <iostream>
using namespace std;

int main() {
    int hp, mana;
    cout << "Введіть HP гравця та ману (через пробіл): ";
    cin >> hp >> mana;

    // Вкладені умови та використання && (Декілька умов одночасно)
    if (hp <= 0) {
        cout << "Гру закінчено. Ви загинули." << endl;
    } 
    else if (hp < 20 && mana >= 50) {
        cout << "ХП критично мало, але є мана! Використовуємо 'Лікування'!" << endl;
    } 
    else if (hp < 20 && mana < 50) {
        cout << "Тікайте! Мало ХП і немає мани." << endl;
    } 
    else {
        cout << "Можна продовжувати бій." << endl;
    }

    // Демонстрація тернарного оператора
    int damage = (hp > 50) ? 20 : 5;
    cout << "Ваш поточний урон: " << damage << endl;

    return 0;
}`,
        codeExplanation: ["`if (hp <= 0)` перевіряє, чи здоров'я впало до нуля або нижче.", "`&&` (Логічне ТА) вимагає, щоб ОБИДВІ умови праворуч і ліворуч від нього були істинними. Якщо хоч одна хибна, весь вираз стає хибним.", "`damage = (hp > 50) ? 20 : 5;` — це тернарний оператор. Якщо `hp > 50`, `damage` стане 20, інакше воно стане 5."],
        practiceTask: 'Зчитайте одне ціле число. Виведіть \'Парне\', якщо воно ділиться на 2 без остачі, або \'Непарне\' інакше.',
        expectedOutput: 'Парне',
        hint: 'Використайте оператор взяття остачі: `if (number % 2 == 0)`',
    },

    {
        id: 'loops',
        title: 'Цикли (for, while, do-while)',
        level: 'beginner',
        xp: 15,
        levelLabel: 'Початківець',
        icon: '🔄',
        videos: [{ "id": "LYIPTmN37SU", "title": "Уроки C++ для початківців / #6 - Цикли та оператори в них" }],
        explanation: `<h3>1. Що таке цикли та DRY?</h3>
<p>Головне правило розробника: <strong>DRY (Don't Repeat Yourself - Не повторюйся)</strong>. Якщо вам треба вивести слово "Привіт" 1000 разів, ви не будете писати 1000 разів <code>cout &lt;&lt; "Привіт";</code>. Для багаторазового повторення блоку коду існують <strong>цикли</strong>.</p>

<h3>2. Цикл while (Поки)</h3>
<p>Використовується, коли ми <strong>НЕ знаємо</strong> точної кількості повторень, і цикл має працювати, <strong>поки</strong> виконується певна умова.</p>
<code>
int n = 10;<br>
while (n > 0) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;cout << n << " ";<br>
&nbsp;&nbsp;&nbsp;&nbsp;n--; // Зменшуємо n, інакше умова ЗАВЖДИ буде n>0 -> нескінченний цикл!<br>
}
</code>

<h3>3. Цикл for (Для)</h3>
<p>Найпопулярніший цикл. Використовується, коли ми чітко знаємо кількість ітерацій (наприклад, пройтися від 1 до 100, або перебрати масив). Він збирає 3 компоненти в одному рядку:</p>
<code>
// for (1.Старт;  2.Умова продовження;  3.Що робити після кожного кроку)<br>
for (int i = 0;   i < 5;              i++) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;cout << i;<br>
}
</code>
<p><em>Порада:</em> Змінна <code>i</code> (від слова index або iterator) існує ТІЛЬКИ всередині циклу \`for\`. Це називається <strong>область видимості (scope)</strong>.</p>

<h3>4. Розрив та Пропуск кроку (break, continue)</h3>
<ul>
  <li><code>break</code> — повністю <strong>вбиває</strong> цикл достроково та виходить з нього. Корисно, коли ви знайшли те, що шукали, і продовжувати цикл немає сенсу.</li>
  <li><code>continue</code> — пропускає <strong>поточний крок</strong> і миттєво стрибає на наступну ітерацію циклу. (Наприклад, перебираємо числа від 1 до 10, але якщо число 5, робимо \`continue\` — 5 буде пропущено).</li>
</ul>

<h3>5. Цикл do-while</h3>
<p>Відрізняється від <code>while</code> тим, що спочатку виконує тіло циклу хоча б <strong>ОДИН РАЗ</strong>, і лише потім перевіряє умову. Ідеально підходить для створення ігрових меню.</p>`,
        code: `#include <iostream>
using namespace std;

int main() {
    // 1. Стандартний цикл FOR
    cout << "Рахуємо до 5: ";
    for (int i = 1; i <= 5; i++) {
        cout << i << " ";
    }
    cout << "\n\n";

    // 2. Цикл WHILE для обробки меню користувача
    int password;
    cout << "Введіть пін-код: ";
    cin >> password;
    
    int attempts = 1;
    while (password != 1234) {
        if (attempts >= 3) {
            cout << "Карту заблоковано! Вичерпано 3 спроби." << endl;
            break; // ДОСТРОКОВИЙ ВИХІД
        }
        cout << "Невірно. Спробуйте ще: ";
        cin >> password;
        attempts++;
    }
    
    if (password == 1234) {
        cout << "Доступ дозволено! Пінкапітал на рахунку." << endl;
    }
    
    // 3. З використанням continue
    cout << "\nНепарні числа від 1 до 6: ";
    for (int j = 1; j <= 6; j++) {
        if (j % 2 == 0) {
            continue; // Якщо число парне, пропускаємо його вивід
        }
        cout << j << " ";
    }
    cout << endl;

    return 0;
}`,
        codeExplanation: ["`for (int i = 1; i <= 5; i++)` — створює лічильник `i`, крутить цикл поки `i<=5`, і на кожному кроці збільшує `i` на одиницю (`i++`).", "`break` зупиняє цикл `while` після 3-х безуспішних спроб.", "`continue` в останньому циклі каже комп'ютеру: 'Якщо j парне, навіть не доходи до cout, а відразу починай наступний крок циклу'."],
        practiceTask: 'Використовуючи цикл for, знайдіть суму всіх чисел від 1 до 100 включно.',
        expectedOutput: '5050',
        hint: 'Створіть `int sum = 0;` перед циклом. В циклі робіть `sum += i;` або `sum = sum + i;`',
    },

    {
        id: 'functions',
        title: 'Функції: Декомпозиція коду',
        level: 'beginner',
        xp: 15,
        levelLabel: 'Початківець',
        icon: '⚙️',
        videos: [{ "id": "VKQ242d-Rag", "title": "Уроки C++ для початківців / #10 – Створення функцій" }],
        explanation: `<h3>1. Що таке функція і навіщо вона потрібна?</h3>
<p>Уявіть, що ви готуєте борщ. Можна змішати всі дії: різати картоплю, терти буряк, варити м'ясо. А можна делегувати це іншим людям. <strong>Функція</strong> — це "міні-програма" всередині вашої програми, яка виконує специфічне невелике завдання. Коли функція знадобиться, ви просто кличете її на ім'я.</p>
<p>Функції допомагають: 
  ✅ Уникати дублювання коду; 
  ✅ Робити код читабельним (код нагадує книгу з розділами); 
  ✅ Легше тестувати та знаходити баги.
</p>

<h3>2. Анатомія функції</h3>
<code>
<span style="color: #569cd6;">тип_повернення</span> <span style="color: #dcdcaa;">назваФункції</span>(<span style="color: #9cdcfe;">параметри/аргументи</span>) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// тіло функції (дії)<br>
&nbsp;&nbsp;&nbsp;&nbsp;return результат;<br>
}
</code>
<br>
<p>Кожна функція має <strong>тип повернення</strong>. Вона як завод — отримує сировину (параметри), обробляє її, і видає готову продукцію. Якщо функція має тип <code>int</code>, вона <strong>зобов'язана</strong> використати ключове слово <code>return {ціле число}</code> в кінці. Якщо функція створена просто щоб щось надрукувати або змінити без повернення результату, як тип повернення вказується <code>void</code> (порожнеча).</p>

<h3>3. Параметри vs Аргументи</h3>
<p><strong>Параметри</strong> — це внутрішні змінні функції (наприклад, <code>int a, int b</code>), які "приймуть" значення, коли функцію викличуть.<br>
<strong>Аргументи</strong> — це фактичні значення (наприклад, <code>5, 10</code>), які ви кидаєте у функцію під час її виклику з \`main()\`.</p>

<h3>4. Локальні та Глобальні змінні</h3>
<p>Змінні, створені всередині будь-якої функції (навіть \`main\`), називаються <strong>локальними</strong>. Вони "народжуються", коли функція починає роботу, і <strong>безповоротно знищуються</strong> в кінці функції (після фігурної дужки \`}\`). Тому параметри \`a\` і \`b\` в різних функціях не конфліктують — це абсолютно різні змінні.</p>

<h3>5. Прототипи (Оголошення)</h3>
<p>Компілятор C++ читає файл зверху вниз. Якщо ви спробуєте викликати функцію \`sum()\` в \`main()\`, а сама функція написана нижче \`main()\`, буде помилка! Рішення: написати код функції НАД \`main()\`, або вказати її "прототип" (заголовок без тіла) над \`main()\`.</p>`,
        code: `#include <iostream>
using namespace std;

// 1. Void функція (нічого не повертає, лише виконує дію)
void printGreeting(string title) {
    cout << "======= " << title << " =======" << endl;
}

// 2. Функція, що повертає значення
// Отримує два числа, повертає більше з них
int findMaximum(int a, int b) {
    if (a > b) {
        return a; // Після return функція негайно завершує роботу!
    } else {
        return b;
    }
}

// 3. Звичайна функція обчислення площі кола
double calculateArea(double radius) {
    const double PI = 3.14159;
    return PI * radius * radius;
}

int main() {
    printGreeting("Початок програми");

    int x = 10;
    int y = 25;
    
    // Виклик функції. Результат зберігається в змінну maxVal
    int maxVal = findMaximum(x, y);
    cout << "Найбільше число: " << maxVal << endl;

    // Можна викликати функцію прямо всередині cout!
    cout << "Площа кола (r=5): " << calculateArea(5.0) << endl;

    printGreeting("Кінець програми");
    return 0;
}`,
        codeExplanation: ["`void printGreeting(string title)` — `void` означає відсутність типу, тому в функції немає `return` значення.", "`int findMaximum(int a, int b)` — функція приймає дві змінні типу `int` і повертає `int`.", "Зверніть увагу: як тільки десь у функції виконується `return`, <strong>функція негайно вимикається</strong>, і будь-який код нижче нього не виконається."],
        practiceTask: 'Напишіть функцію `double multiply(double a, double b)`, яка приймає два числа і повертає їхній добуток. Викличте її в main і виведіть результат для 4 та 2.5.',
        expectedOutput: '10',
        hint: 'У тілі функції: `return a * b;`. В main: `cout << multiply(4, 2.5);`',
    },

    {
        id: 'arrays',
        title: 'Масиви та Рядки',
        level: 'beginner',
        xp: 20,
        levelLabel: 'Початківець',
        icon: '📊',
        videos: [{ "id": "LXsUHdsDWW4", "title": "Уроки C++ для початківців / #7 – Масиви даних (одновимірні)" }],
        explanation: `<h3>1. Що таке Масиви?</h3>
<p>Уявіть, що ви пишете гру і вам треба зберегти рекорди 10 гравців. Створювати змінні \`int score1, score2, score3...\` незручно. <strong>Масив (Array)</strong> — це наче потяг, де багато вагонів з однаковим вантажем, зчеплених один за одним у пам'яті комп'ютера. Усі елементи масиву обов'язково <strong>одного типу</strong>.</p>
<code>int scores[5] = {90, 85, 78, 100, 50};</code>
<p>У цьому масиві 5 комірок.</p>

<h3>2. Золоте правило масивів: Індексація з Нуля!</h3>
<div class="note-box">
  ⚠️ <strong>Увага:</strong> В C++ (як і майже в усіх мовах), рахунок об'єктів у масиві починається з <strong>0</strong>, а не з 1! Тому масив на 5 елементів має індекси <code>0, 1, 2, 3, 4</code>. Якщо ви спробуєте звернутися до \`scores[5]\`, ви випадково зайдете в чужу пам'ять і отримаєте "сміттєве" непередбачуване значення, або програма впаде (Segmentation Fault).
</div>

<h3>3. Багатовимірні масиви (Матриці)</h3>
<p>Для зберігання двовимірних даних (наприклад, поле гри Tic-Tac-Toe або шахова дошка) використовують масиви масивів (матриці).</p>
<code>int grid[3][3]; // Матриця 3x3</code><br>
<p>Перша цифра – рядок, друга – стовпчик. Робота з ними зазвичай вимагає використання двох вкладених циклів <code>for</code>.</p>

<h3>4. Рядки (std::string) як масив символів</h3>
<p>Тип <code>string</code> під капотом є, по суті, розумним динамічним масивом літер (<code>char</code>). Тому до букв в рядку можна звертатися так само через квадратні дужки \`[ ]\`.</p>
<p>Основні методи (функції) рядків:</p>
<ul>
  <li><code>word.length()</code> — кількість символів.</li>
  <li><code>word.substr(idx, count)</code> — вирізати підрядок починаючи з \`idx\` кількостю \`count\`.</li>
  <li><code>word.find("тест")</code> — знайти позицію слова "тест".</li>
</ul>`,
        code: `#include <iostream>
#include <string>
using namespace std;

int main() {
    // 1. Статичний масив з 5 елементів
    int scores[5] = {90, 85, 78, 92, 88};

    // Алгоритм пошуку максимального значення
    int maxScore = scores[0]; // Припускаємо, що перший - найбільший
    for (int i = 1; i < 5; i++) {
        if (scores[i] > maxScore) {
            maxScore = scores[i]; // Оновлюємо, якщо знайшли більший
        }
    }
    cout << "Рекорд: " << maxScore << endl;

    // 2. Двовимірний масив (матриця 2 рядки, 3 стовпці)
    int map[2][3] = {
        {1, 2, 3},
        {4, 5, 6}
    };
    cout << "Елемент [1][2] (другий рядок, третій стовпець): " << map[1][2] << endl;

    // 3. Робота з string як з масивом
    string word = "Programming";
    cout << "Слово містить " << word.length() << " літер." << endl;
    cout << "Перша літера: " << word[0] << endl;
    cout << "Остання літера: " << word[word.length() - 1] << endl; // НІКОДИ не вказуйте word.length(), розмір-1!

    return 0;
}`,
        codeExplanation: ["Масив оголошується: `тип назва[розмір]`. Якщо ми одразу пишемо `{ ... }`, розмір можна не вказувати — `int arr[] = {1,2};`", "Щоб звернутись до останнього елемента рядка (чи масиву), необхідно брати індекс `розмір-1`.", "`map[1][2]` значить: взяти масив з індексом 1 (це другий), і в ньому елемент з індексом 2 (це третій)."],
        practiceTask: 'Створіть масив `int nums[3] = {10, 20, 30};`. Знайдіть їх суму, перебравши значення циклом for.',
        expectedOutput: '60',
        hint: 'Створіть `sum=0`, у циклі `for (int i=0; i<3; i++)` робіть `sum += nums[i];`',
    },

    {
        id: 'pointers',
        title: 'Покажчики (Pointers) та Пам\'ять',
        level: 'intermediate',
        xp: 25,
        levelLabel: 'Середній',
        icon: '👉',
        videos: [{ "id": "6GEzlpDxaos", "title": "C++ ⦁ Урок 10 ⦁ Покажчики" }],
        explanation: `<h3>1. Що таке адреса в пам'яті?</h3>
<p>Оперативна пам'ять комп'ютера (ОЗП / RAM) розділена на мільйони маленьких "комірок", кожна з яких вміщує 1 байт. У кожної комірки є свій унікальний <strong>адрес</strong> (порядковий номер шістнадцятковою системою, наприклад <code>0x7ffee1f5</code>). Коли ви створюєте змінну <code>int x = 5;</code>, програма резервує 4 байти підряд десь в пам'яті.</p>

<h3>2. Сутність покажчика (Pointer)</h3>
<p><strong>Покажчик (Pointer)</strong> — це особлива змінна, яка містить не число 5 чи рядок "Hello", а <strong>АДРЕСУ в пам'яті іншої змінної</strong>. Він буквально "вказує" на те місце, де лежать дані.</p>
<p>Для роботи з покажчиками є два магічних оператори:</p>
<ul>
  <li><code>&</code> (Амперсанд) — <strong>Оператор взяття адреси.</strong> Взяти адресу існуючої змінної. (Читаємо: "адреса...").</li>
  <li><code>*</code> (Зірочка) — <strong>Оператор розіменування.</strong> Піти за адресою і взяти або змінити значення, яке там лежить. (Читаємо: "значення за адресою...").</li>
</ul>

<div class="note-box">
  💡 <strong>Аналогія:</strong> <code>x</code> — це ваш сейф із грошима.<br>
  <code>&x</code> — це адреса банку та номер сейфу.<br>
  Покажчик <code>ptr</code> — це папірець, на якому записана адреса <code>&x</code>.<br>
  Розіменування <code>*ptr</code> — це коли ви берете папірець, їдете за цією адресою, відкриваєте сейф і дивитесь скільки там грошей!
</div>

<h3>3. Навіщо це все? (Передача за посиланням)</h3>
<p>Раніше ми вивчали функції. Коли функція отримує свій аргумент \`function(int a)\`, вона створює повну ОДНОРАЗОВУ КОПІЮ. Зміните \`a\` у функції — оригінал не зміниться. Якщо ж передати у функцію покажчик! Тоді функція буде знати, де лежить оригінал, і зможе поїхати до нього і змінити його прямо в пам'яті!</p>

<h3>4. Безпека: nullptr</h3>
<p>Якщо покажчик просто оголосити \`int* p;\`, він міститиме якесь "сміття" і вказуватиме в випадкове місце в пам'яті комп'ютера. Якщо ви розіменуєте такий покажчик (\`*p = 10;\`), ви пошкодите чужу пам'ять і програма "крашнеться". Заведіть звичку: якщо покажчик поки нікуди не має вказувати, присвоюйте йому <code>nullptr</code> (безпечний "нуль" в C++11).</p>`,
        code: `#include <iostream>
using namespace std;

// Функція змінює змінні через покажчики! 
// Ми передаємо АДРЕСИ (int* pa) і змінюємо оригінали через зірочку (*pa).
void pushMoney(int* target, int amount) {
    // Йдемо за адресою цілі і додаємо amount до того, що там лежить
    *target = *target + amount; 
}

int main() {
    int chest = 42;             // Сейф з 42 монетами
    int* ptr = &chest;          // Покажчик, зберігає АДРЕСУ chest

    cout << "В сейфі лежить: " << chest << " монет." << endl;
    cout << "Адреса сейфа в RAM: " << &chest << endl;
    cout << "Покажчик ptr містить: " << ptr << endl;
    
    // Розіменування: дивимось що там лежить
    cout << "Через покажчик можна змінити: *ptr = 100!" << endl;
    *ptr = 100;
    
    cout << "Тепер в сейфі: " << chest << " монет." << endl; // Змінився оригінал!

    cout << "\n---- Функції та Покажчики ----" << endl;
    int wallet = 50;
    cout << "Гаманець до виклику: " << wallet << endl;
    
    // Передаємо АДРЕСУ гаманця, а не його копію
    pushMoney(&wallet, 500);
    
    cout << "Гаманець після: " << wallet << endl;

    int* safePtr = nullptr; // Правильна звичка
    
    return 0;
}`,
        codeExplanation: ["`int* ptr` визначає змінну типу 'покажчик на ціле число'.", "При виклику `pushMoney(&wallet, 500)` ми кидаємо адресу вказівнику `target` у функції.", "`*target = *target + amount;` означає: подивись, що лежить за адресою `target`, додай до нього `amount` і запиши НОВЕ значення назад за цією ж адресою."],
        practiceTask: 'Створіть цілу змінну x=10. Створіть покажчик на неї. Використовуючи ТІЛЬКИ оператор розіменування (*), збільште x ще на 10, і виведіть.',
        expectedOutput: '20',
        hint: 'int* p = &x; Потім зробіть *p = *p + 10; Або *p += 10; cout << *p;',
    },

    {
        id: 'dynamic_memory',
        title: 'Стек, Купа та Динамічна пам\'ять',
        level: 'intermediate',
        xp: 25,
        levelLabel: 'Середній',
        icon: '🧠',
        videos: [{ "id": "d0jHHhSZ6Yc", "title": "#24 Динамічна пам'ять. Оператори new та delete" }],
        explanation: `<h3>1. Стек (Stack) vs Купа (Heap)</h3>
<p>Відкриємо секрет: коли програма запускається, її пам'ять поділена на кілька областей. Дві найголовніші — це <strong>Стек</strong> (Stack) та <strong>Купа</strong> (Heap).</p>
<ul>
  <li>📦 <strong>Стек (Автоматична пам'ять):</strong> Тут зберігаються всі звичайні змінні (\`int a\`, локальні змінні в функціях). Її розмір жорстко обмежений (часто 1-2 МБ). Змінні сюди швидко додаються і видаляються САМІ, коли функція завершується. Ви не можете створити масив на мільйон елементів на стеку — буде <em>Stack Overflow</em>.</li>
  <li>🏔️ <strong>Купа (Динамічна пам'ять):</strong> Гігантський океан пам'яті (усі ваші Гігабайти оперативки). Тут пам'ять надійна і велика. Але вона <strong>РУЧНА</strong>. Ви самі маєте попросити її у ОС, і ви <strong>ЗОБОВ'ЯЗАНІ</strong> віддати її назад.</li>
</ul>

<h3>2. Оператори new та delete</h3>
<p>Щоб "забронювати" пам'ять на Купі, в C++ існує оператор <code>new</code>. Він шукає вільне місце, виділяє пам'ять і <strong>повертає покажчик</strong> на цей новий об'єкт.</p>
<code>
int* p = new int; // Базова змінна на купі<br>
int* arr = new int[50000]; // Масив на 50 000 елементів
</code>

<div class="note-box">
  🔥 <strong>Увага: Правило пам'яті номер один!</strong><br>
  C++ не має вбудованої "домогосподарки" (Garbage Collector), як Java чи Python. Усе, що було виділено через \`new\`, повинно бути ВРУЧНУ знищено через <code>delete</code>.
  Якщо ви цього не зробите, ця пам'ять буде вважатися зайнятою аж до закриття програми! Це називається <strong>Витік пам'яті (Memory Leak)</strong>.
</div>
<code>
delete p;     // Для однієї змінної<br>
delete[] arr; // Для масивів ОБОВ'ЯЗКОВО з квадратними дужками
</code>

<h3>3. "Висячі" покажчики (Dangling Pointers)</h3>
<p>Після \`delete ptr;\` пам'ять повертається системі, але в самій змінній \`ptr\` досі лежить старий адрес! Якщо ви випадково підете за ним \`*ptr = 10\`, програма зламається. Тому після видалення завжди присвоюйте: <code>ptr = nullptr;</code>.</p>`,
        code: `#include <iostream>
using namespace std;

int main() {
    // 1. Створення єдиної змінної на Купі та ініціалізація значенням 42
    int* dataPointer = new int(42); 
    cout << "Значення на купі: " << *dataPointer << endl;
    
    // Ручне звільнення пам'яті
    delete dataPointer;
    dataPointer = nullptr; // Запобігаємо Dangling Pointer
    
    // 2. Створення великого масиву. 
    // Розмір динамічного масиву можна задавати змінною (на Stack це заборонено!)
    int size;
    cout << "Введіть розмір масиву: ";
    cin >> size;
    
    // Виділяємо пам'ять
    int* dynamicArr = new int[size];
    
    // Працюємо як зі звичайним масивом
    for (int i = 0; i < size; i++) {
        dynamicArr[i] = (i + 1) * 10; 
    }
    
    cout << "Перший і останній елемент: " 
         << dynamicArr[0] << " ... " << dynamicArr[size - 1] << endl;
         
    // 3. Звільнення пам'яті масиву. Обов'язково []
    delete[] dynamicArr;
    dynamicArr = nullptr;

    return 0;
}`,
        codeExplanation: ["`new int(42)` — виділяє місце під 1 int і відразу кладе туди 42. Повертає адресу.", "`int* dynamicArr = new int[size]` — виділяє блок пам'яті під `size` елементів. `dynamicArr` зберігає адресу першого елемента.", "Якщо масив виділено через `new ...[]`, то й видаляти його НАЗАВЖДИ треба через `delete[]`, інакше знищиться тільки перший елемент, а решта буде витікати."],
        practiceTask: 'Виділіть масив пам\'яті з 3 чисел на купі. Запишіть у них 10, 20 та 15. Знайдіть суму та відразу застосуйте \'delete[]\'.',
        expectedOutput: '45',
        hint: 'Зовсім як в коді зверху. new int[3]... sum... delete[]',
    },

    {
        id: 'file_io',
        title: 'Робота з файлами',
        level: 'intermediate',
        xp: 30,
        levelLabel: 'Середній',
        icon: '📁',
        videos: [{ "id": "eBBjRdCE4KA", "title": "C++ ⦁ Урок 15 ⦁ Використання файлової системи - ifstream" }],
        explanation: `<h3>1. Бібліотека &lt;fstream&gt;</h3>
<p>Для роботи з файловими системами в C++ використовується стандартна бібліотека <code>&lt;fstream&gt;</code> (File Stream). Аналогічно до <code>cout</code> та <code>cin</code>, ми працюємо з потоками даних, але тепер вони підключені не до екрану комп'ютера, а до файлу на жорсткому диску.</p>

<h3>2. Три кити файлів</h3>
<ul>
  <li><code>ofstream</code> (Output File Stream) — Потік для <strong>запису</strong> (створення) у файл.</li>
  <li><code>ifstream</code> (Input File Stream) — Потік для <strong>читання</strong> файлу.</li>
  <li><code>fstream</code> — Універсальний потік, який може і читати, і писати водночас.</li>
</ul>

<h3>3. Запис у файл</h3>
<p>Коли ви відкриваєте файл через <code>ofstream</code>, якщо файлу немає, він <strong>буде створений</strong>. Якщо він вже існує — його попередній вміст буде повністю стертий і переписаний новим! (Щоб цьогоникнути, треба використовувати спеціальний режим додавання <code>ios::app</code> - Append).</p>
<code>
ofstream file("data.txt");<br>
file << "Hello File" << endl;<br>
file.close();  // ЗАКРИТТЯ! Золоте правило.
</code>

<h3>4. Читання тексту (З уникненням помилок)</h3>
<p>Зчитувати дані з файлу можна по одному слову через <code>&gt;&gt;</code> або по цілому рядку в циклі <code>while (getline(файл, змінна))</code>. Це найбезпечніший спосіб читати до самого останнього символу (кінця файлу).</p>

<div class="note-box">
  ⚠️ <strong>Важливо:</strong> Файли не завжди відкриваються. Може не вистачити прав адміністратора, диск може бути пошкодженим або назва файлу написана з помилкою. ЗАВЖДИ перевіряйте стан потоку методом <code>if (!file.is_open()) { ... }</code>.
</div>`,
        code: `#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    // ================= 1. ЗАПИС У ФАЙЛ =================
    // Створюємо потік виводу. Файл створиться автоматично (в папці з програмою)
    ofstream outFile("players.txt");
    
    // Перевірка на успіх
    if (!outFile.is_open()) {
        cout << "Помилка! Диск пошкоджено або немає прав." << endl;
        return 1; // Код помилки операційній системі
    }

    // Пишемо за допомогою того самого оператора <<
    outFile << "Scorpion_Subzero 500" << endl;
    outFile << "Raiden_God 900" << endl;
    
    outFile.close(); // ЗНИЩИТИ потік, розблокувати файл для Windows!
    cout << "[Файл успішно записано]\n\n";

    // ================= 2. ЧИТАННЯ З ФАЙЛУ =================
    ifstream inFile("players.txt");
    
    if (!inFile.is_open()) {
        cout << "Файл не знайдено!" << endl;
        return 1;
    }

    cout << "--- Вміст файлу ---" << endl;
    string line;
    // getline читає по одному РЯДКУ і повертає ПРАВДУ поки не кінець файлу (EOF)
    while (getline(inFile, line)) {
        cout << "Прочитано рядок: " << line << endl;
    }

    inFile.close(); // Не забуваємо закривати

    return 0;
}`,
        codeExplanation: ["`ofstream outFile(\"players.txt\")` намагається відкрити файл. За замовчуванням зітре старе наповнення.", "`!outFile.is_open()` — дуже важлива перевірка. Ніколи не припускайте, що файл є ідеально готовим до запису.", "`while (getline(inFile, line))` висмоктує файл рядок за рядком, аж поки останній рядок не поверне статус False (показуючи, що досягнуто кінця файлу EOF)."],
        practiceTask: 'Запишіть своє Ім\'я стрічкою через ofstream. А потім закрийте, відкрийте через ifstream і виведіть на екран.',
        expectedOutput: 'Ваше Ім\'я',
        hint: 'Два етапи: 1) ofstream out("t.txt"); out<< "Ім\'я"; out.close(); 2) ifstream in("t.txt"); getline(in, str); cout << str;',
    },

    {
        id: 'recursion',
        title: 'Рекурсія',
        level: 'intermediate',
        xp: 30,
        levelLabel: 'Середній',
        icon: '🪆',
        videos: [{ "id": "Doue2y4K6wY", "title": "#22 Рекурсія" }, { "id": "xqVYIkYwqUc", "title": "C++ рекурсія" }],
        explanation: `<h3>1. Що таке рекурсія?</h3>
<p>Слово "рекурсія" часто викликає жах у новачків, але насправді це просто красивий математичний трюк. <strong>Рекурсія</strong> — це коли функція викликає САМУ СЕБЕ як частину свого виконання.</p>
<p>Уявіть російську матрьошку: щоб розібрати велику матрьошку, ви знаходите всередині меншу і застосовуєте ту ж саму дію ("Відкрити"). Це повторюється, поки не дійдете до найменшої, цільної фігурки, яку вже відкрити не можна.</p>

<h3>2. Дві складові будь-якої рекурсії:</h3>
<p>Для того, щоб функція не викликала себе ДО НЕСКІНЧЕННОСТІ і не призвела до краху комп'ютера (Stack Overflow), їй необхідні дві речі:</p>
<ol>
  <li><strong>Базовий випадок (Base Case):</strong> Це умова-гальмо. Момент, коли функція більше не буде викликати себе, а скаже <em>"Досить, я повертаю результат!"</em> (Це та цільна матрьошка).</li>
  <li><strong>Рекурсивний крок:</strong> Процес розбиття великого завдання на одне маленьке завдання + виклик себе ж, але з меншим аргументом (<code>n-1</code>).</li>
</ol>

<h3>3. Класичний приклад: Факторіал</h4>
<p>Факторіал числа 5 (записується як \`5!\`) — це множення: <code>5 * 4 * 3 * 2 * 1 = 120</code>.</p>
<p>Математично це можна розписати так: <code>5! = 5 * 4!</code>. А 4! це <code>4 * 3!</code>. І так далі.</p>
<p>Спробуємо:
<code><br>
int fact(int n) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;if (n == 1) return 1;    // Гальмо: 1! завжди 1<br>
&nbsp;&nbsp;&nbsp;&nbsp;return n * fact(n - 1);  // Крок: я помножую поточне число на факторіал ПОПЕРЕДНЬОГО<br>
}<br>
</code>
</p>

<div class="note-box">
  ⚠️ <strong>Небезпека (Stack Overflow):</strong> Кожений виклик функції займає місце в стековій пам'яті. Якщо ви забудете написати умову <code>if (n == 1)</code>, функція почне вважати факторіали -1, -2, -3... нескінченно. Пам'ять стеку заповниться, і програма "вибухне"!
</div>`,
        code: `#include <iostream>
using namespace std;

// Функція обчислення факторіалу
long long factorial(int n) {
    // 1. Базовий випадок (Кінець рекурсії)
    if (n <= 1) {
        return 1;
    }
    // 2. Рекурсивний крок
    // (Позначили зірочку. Будемо чекати поки return не поверне результат)
    return n * factorial(n - 1);
}

// Функція зворотного відліку
void countdown(int t) {
    if (t <= 0) {
        cout << "Пуск РАКЕТИ!" << endl;
        return; // Гальма. Завершуємо цю гілку.
    }
    cout << "До старту: " << t << "..." << endl;
    
    // Викликаємо саму себе зі зменшеним значенням
    countdown(t - 1);
}

int main() {
    cout << "Рахуємо 5! (Факторіал): " << factorial(5) << endl;
    
    cout << "\n==== Запуск ====" << endl;
    countdown(3);

    return 0;
}`,
        codeExplanation: ["`if (n <= 1) return 1;` — найважливіший рядок! Він гарантує, що глибокий цикл занурення зупиниться.", "Як працює виклик `factorial(5)`? Він чекає `5 * factorial(4)`. В свою чергу `factorial(4)` чекає `4 * factorial(3)`... і так далі, поки не впреться у 1, а потім все збирається назад, як ланцюжок: `1 * 2 * 3 * 4 * 5`.", "Рекурсія може працювати трохи повільніше циклу `for`, але дозволяє компактно вирішувати задачі обходу дерев, папок та лабіринтів."],
        practiceTask: 'Напишіть рекурсивну функцію power(int base, int exp), що підносить число (base) в степінь (exp). Підказка: базовий випадок if(exp == 0) return 1.',
        expectedOutput: '8',
        hint: 'У функції повертайте `base * power(base, exp - 1)`. Викличте cout << power(2,3);',
    },

    {
        id: 'classes',
        title: 'Класи та Об\'єкти. Основа ООП',
        level: 'oop',
        xp: 40,
        levelLabel: 'ООП (Новачок)',
        icon: '🏛️',
        videos: [{ "id": "25IUjI8Bf5k", "title": "Уроки C++ для початківців / #19 – Створення класів та об'єктів" }, { "id": "VaaMEQSQx8I", "title": "1. С++ Українською  Класи та об'єкти" }],
        explanation: `<h3>1. Що таке ООП? (Об'єктно-Орієнтоване Програмування)</h3>
<p>Коли ви пишете маленьку програму (калькулятор), достатньо використовувати звичайні змінні та функції (Процедурне програмування). Але коли ви розробляєте <strong>велику відеогру</strong> або банківську систему, змінних стає тисячі. 
Тоді на допомогу приходить <strong>ООП</strong> — підхід, де ми моделюємо реальний світ за допомогою "Об'єктів".</p>
<p>В ООП все є об'єктом. Гравець є об'єктом. Зброя є об'єктом. Вікно програми є об'єктом. І кожен об'єкт відповідає за <strong>СЕБЕ САМОГО</strong>: він має свої дані та функції, що можуть міняти ці дані.</p>

<h3>2. Клас vs Об'єкт (Креслення vs Будинок)</h3>
<ul>
  <li>📝 <strong>Клас (Class)</strong> — це креслення. Це шаблон, опис того, з чого складатиметься Об'єкт. <em>Клас "Автомобіль" описує, що авто має колір, марку і швидкість. Але сам Клас не можна водити! Це лише ідея на папері.</em></li>
  <li>🏠 <strong>Об'єкт (Object)</strong> — це конкретний екземпляр класу. Це справжній будинок, побудований за кресленням. Справжнє червоне авто Toyota зі швидкістю 150.</li>
</ul>

<h3>3. Будова Класу: Поля та Методи</h3>
<p>Будь-який клас в C++ складається з двох речей:</p>
<ol>
  <li><strong>Поля (Дані / Атрибути)</strong> — це просто звичайні змінні, які "належать" класу. В "Гравця" це <code>int hp, int attack</code>. (<em>СТАН</em> об'єкта)</li>
  <li><strong>Методи (Функції)</strong> — це звичайні функції, які можуть щось робити з Полями. Наприклад <code>void heal() { hp += 10; }</code>. (<em>ПОВЕДІНКА</em> об'єкта)</li>
</ol>

<h3>4. Створення класу (Синтаксис)</h3>
<p>Створення розпочинається з ключового слова <code>class</code>.</p>
<code>
class Player {<br>
public:<br>
&nbsp;&nbsp;&nbsp;&nbsp;string name;<br>
&nbsp;&nbsp;&nbsp;&nbsp;int health;<br>
&nbsp;&nbsp;&nbsp;&nbsp;<br>
&nbsp;&nbsp;&nbsp;&nbsp;void takeDamage(int dmg) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;health -= dmg;<br>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;cout << name << " втратив " << dmg << " хп!";<br>
&nbsp;&nbsp;&nbsp;&nbsp;}<br>
}; // НЕ ЗАБУВАЙМО КРАПКУ З КОМОЮ ПІСЛЯ КЛАСУ!
</code>

<div class="note-box">
  💡 <strong>Що таке \`public:\`?</strong> Це специфікатор доступу. Він каже: "Усі ці змінні та функції знаходяться у публічній зоні. Будь-який програміст може звернутися до них ззовні (з \`main()\`) і змінити їх". Без \`public\` клас автоматично закриває свої дані на замок (private).
</div>`,
        code: `#include <iostream>
#include <string>
using namespace std;

// 1. Створюємо КРЕСЛЕННЯ (Клас)
// Краще називати класи з Великої Літери
class Student {
public: // Відкриваємо доступ для main()
    // --- ПОЛЯ --- (Дані студента)
    string name;
    int age;
    double gpa;

    // --- МЕТОДИ --- (Поведінка студента)
    void showInfo() {
        cout << "Студент: " << name << ", Вік: " << age << endl;
        cout << "Середній бал: " << gpa << "\n" << endl;
    }

    bool isExcellent() {
        if (gpa >= 4.5) return true;
        return false;
    }
};

int main() {
    // 2. Створюємо ОБ'ЄКТ (Реального студента) за кресленням "Student"
    Student s1;
    
    // Доступ до полів здійснюється через КРАПКУ '.'
    s1.name = "Марія Осадча";
    s1.age = 19;
    s1.gpa = 4.8;

    // Створюємо ДРУГОГО студента. Вони повністю незалежні!
    Student s2;
    s2.name = "Олег";
    s2.age = 21;
    s2.gpa = 3.2;

    cout << "--- Список ---" << endl;
    // Викликаємо методи для КОЖНОГО об'єкта окремо
    s1.showInfo();
    s2.showInfo();
    
    if (s1.isExcellent()) {
        cout << s1.name << " отримує підвищену стипендію!" << endl;
    }

    return 0;
}`,
        codeExplanation: ["`class Student { ... };` — створює новий тип даних `Student` (закінчується крапкою з комою!).", "`public:` дозволяє звертатися до полів (наприклад, `s1.name`) прямо звідси, ззовні.", "`Student s1;` — створює один реальний екземпляр студента в пам'яті комп'ютера. Усі його змінні поки що порожні (мінус мільярди або пусті рядки).", "Щоб залізти в середину об'єкта, використовуйте крапку: `s1.gpa = 4.8;` Обов'язкова умова для об'єктного програмування."],
        practiceTask: 'Створіть свій власний клас \'Car\' з полями brand(string), year(int), speed(int). У main створіть 1 об\'єкт і присвойте Toyota, 2022 рік, 150 швидкість.',
        expectedOutput: 'Марка: Toyota\nРік: 2022',
        hint: 'Після `class Car { public: ... };` створіть `Car myCar; myCar.brand = ...`',
    },

    {
        id: 'constructors',
        title: 'Конструктори та Деструктори',
        level: 'oop',
        xp: 40,
        levelLabel: 'ООП (Новачок)',
        icon: '🏗️',
        videos: [{ "id": "lC2iJy1qIYw", "title": "Уроки C++ для початківців / #20 – Конструктори, деструктори" }],
        explanation: `<h3>1. Що таке Конструктор? Навіщо він?</h3>
<p>В попередньому уроці ми створили \`Student s1;\` і потім ПИШУЧИ РУКАМИ, по одній стрічці задавали <code>s1.name = "..."; s1.age = 19;</code>. Якщо в нас 5 полів даних, створення одного гравця займатиме 5 рядків коду! А якщо ми забудемо вказати \`hp\`?</p>
<p>Тут на допомогу приходить <strong>Конструктор</strong>. Це спеціальний метод, який ВИКЛИКАЄТЬСЯ САМ ОДРАЗУ під час створення об'єкта. Він "готує" об'єкт до життя: видає стартову броню, задає ім'я, розставляє параметри.</p>

<h3>2. Правила Конструктора:</h3>
<ol>
  <li>Назва конструктора має бути <strong>ТОЧНІСІНЬКО ТАКА Ж</strong>, як назва класу.</li>
  <li>Він <strong>НІЧОГО</strong> не повертає (навіть \`void\` писати не можна).</li>
  <li>Може бути декілька різних конструкторів! (Наприклад один без параметрів — для стандартного моба, і другий з параметрами — для боса імені "Король Ліч"). Це називається <em>Перевантаження методів (Method Overloading)</em>.</li>
</ol>

<h3>3. Список ініціалізації (\`: поле(значення)\`)</h3>
<p>Сучасна професійна практика C++ — ініціалізувати змінні в конструкторі через двокрапку. Це працює швидше.</p>
<code>
Player(string n, int h) : name(n), health(h) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Тіло конструктора залишається пустим, або тут можна надрукувати "Створено гравця"<br>
}
</code>
<p>Отже, тепер ви зможете викликати це так: <code>Player p1("Arthur", 100);</code> — Ідеально, швидко і лише в 1 рядок!</p>

<h3>4. Що таке Деструктор? (Добиваємо об'єкт)</h3>
<p>Якщо Конструктор запускається під час народження об'єкта, то <strong>Деструктор</strong> — перед самою смертю (коли об'єкт видаляється з пам'яті: або завершується функція, або використовується \`delete\`). Класичне застосування Деструктора — вимкнути з'єднання з Базою Даних або викликати \`delete[]\` для власних динамічних масивів класу, щоб не було витоку пам'яті.</p>
<p>Пишеться з Тільдою: <code>~Player() { cout &lt;&lt; "Мене вбито!"; }</code></p>`,
        code: `#include <iostream>
#include <string>
using namespace std;

class Book {
public:
    string title;
    string author;
    int year;

    // 1. Конструктор ЗА ЗАМОВЧУВАННЯМ (Без параметрів)
    // Якщо користувач створить просто Book b1;
    Book() : title("Без назви"), author("Невідомий"), year(0) {
        cout << "[Збірка] Створена пуста книга" << endl;
    }

    // 2. Конструктор З ПАРАМЕТРАМИ
    // Спеціальний швидкий список ініціалізації:
    Book(string t, string a, int y) : title(t), author(a), year(y) {
        cout << "[Збірка] Книгу '" << title << "' надруковано!" << endl;
    }

    // 3. Конструктор копіювання (якщо просто копіюємо іншу книгу)
    Book(const Book& otherBase) {
        title = otherBase.title + " (Копія)";
        author = otherBase.author;
        year = otherBase.year;
    }

    // 4. ДЕСТРУКТОР. Знищує книгу після закінчення програми (чи блоку {})
    ~Book() {
        cout << "[Смітник] Книга '" << title << "' згоріла..." << endl;
    }

    void show() {
        cout << " -> " << title << " | Автор: " << author 
             << " | Випущено: " << year << endl;
    }
};

int main() {
    cout << "=== Початок Бібліотеки ===" << endl;

    Book b1; // Викличе перший конструктор
    b1.show();
    
    Book b2("Кобзар", "Тарас Шевченко", 1840); // Викличе другий!
    b2.show();
    
    Book b3 = b2; // Викличе третій (копіювання)
    b3.show();

    cout << "=== Закриваємо програму ===" << endl;
    // Відразу після return всі змінні зі стеку будуть знищені НАВПАКИ! 
    // b3 згорить, потім b2, потім b1 (принцип LIFO). 
    return 0;
}`,
        codeExplanation: ["`Book() : title...` — конструктори завжди мають ту саму назву. Коли ви пишете просто `Book b1;`, компілятор йде сюди і ставить 'Без назви'.", "У списку ініціалізації: ліворуч від дужок — поле класу, в дужках — аргумент (`title(t)`). Це еквівалент `this->title = t;`", "Деструктор `~Book()` викликається АВТОМАТИЧНО. Вам ніколи не треба писати `b2.~Book()`. Система сама прибере за вами."],
        practiceTask: 'Створіть клас \'BankAccount\' з конструктором BankAccount(string owner, double balance) зі списком ініціалізації. Виведіть \'Обліковий запис створено\'.',
        expectedOutput: 'Обліковий запис створено\nIvan: 1000',
        hint: 'Конструктор: `BankAccount(string o, double b) : owner(o), balance(b) { ... }`',
    },

    {
        id: 'encapsulation',
        title: 'Інкапсуляція. Public та Private',
        level: 'oop',
        xp: 40,
        levelLabel: 'ООП (Середній)',
        icon: '🛡️',
        videos: [{ "id": "uReH0dDW568", "title": "C++ інкапсуляція" }],
        explanation: `<h3>1. Що таке Інкапсуляція? І навіщо ці таємниці?</h3>
<p>До цього моменту всі наші змінні були <code>public</code>. Уявіть клас <code>BankAccount</code>, де <code>double balance;</code> стоїть відкрито публічно. </p>
<p>Будь-хто (або ваш колега-програміст в іншій частині коду) може просто взяти об'єкт і написати: <code>account.balance = -9999999;</code>.<br>
Все. Банк банкрутує. Змінну зламано некоректним значенням (баланс не може бути мінусовим просто так). </p>

<p><strong>Інкапсуляція</strong> — це принцип приховування внутрішніх чутливих даних об'єкта за "непробивною стіною", щоб ніхто випадково або спеціально не зміг їх зіпсувати прямо із <code>main()</code>.</p>

<h3>2. Три кити доступу (Private, Public, Protected)</h3>
<ul>
  <li>🔴 <strong><code>private</code> (Приватний)</strong> — Ніхто ззовні класу не бачить ці змінні. Спроба написати <code>a.balance</code> в <code>main</code> викличе жорстку ПОМИЛКУ КОМПІЛЯТОРА. Тільки самі внутрішні методи класу знають про ці змінні. За замовчуванням у C++ (якщо нічого не писати), у класі \`class\` все є \`private\`. Структури (\`struct\`) ж навпаки - \`public\`.</li>
  <li>🟢 <strong><code>public</code> (Публічний)</strong> — Повний доступ звідки-завгодно для читання та перезаписування.</li>
  <li>🟡 <strong><code>protected</code> (Захищений)</strong> — Як приватний, але видимий "дітям" у разі успадкування (поговоримо про це у наступних уроках).</li>
</ul>

<h3>3. Золотий стандарт: Геттери та Сеттери</h3>
<p>Якщо все приватне, як дізнатися або змінити баланс логічним шляхом? За допомогою <strong>Спеціальних публічних методів:</strong></p>
<ol>
  <li><strong>Getter:</strong> Метод що тільки читає і повертає поточне значення. <code>double getBalance() { return balance; }</code>. Він не змінює нічого, лише віддає. То ж ми не переживаємо.</li>
  <li><strong>Setter:</strong> Метод що приймає аргумент, <strong>ПЕРЕВІРЯЄ</strong> або обробляє його (Валідація), і лише тоді записує в <code>private</code> змінну.</li>
</ol>
<div class="note-box">
  💎 <strong>Найкраща практика (Best Practice):</strong> В професійному C++ <strong>абсолютно УСІ поля класу</strong> мають бути в зоні \`private\`. Ззовні ви відкриваєте доступ лише через \`Get\`... і \`Set\`... (або інші осмислені методи, як от \`deposit()\`).
</div>`,
        code: `#include <iostream>
#include <string>
using namespace std;

class BankAccount {
// === ЗАКРИТО ДЛЯ ВСІХ ЗОВНІ ===
private:
    string owner;
    double balance;
    
    // Внутрішній метод. Його НЕ можна викликати з main(). Тільки інші методи класу!
    void triggerAlarm() {
        cout << "[ТРИВОГА] Зафіксована спроба хакерства на рахунку " << owner << "!" << endl;
    }

// === ВІДКРИТО ДЛЯ ВЗАЄМОДІЇ ===
public:
    // Конструктор
    BankAccount(string o, double b) : owner(o), balance(b) {}

    // GETTER - (видача даних безпечно)
    double getBalance() { 
        return balance; 
    }
    string getOwner() { 
        return owner; 
    }

    // SETTER (модифікація даних з жорсткою ПЕРЕВІРКОЮ)
    void deposit(double amount) {
        if (amount <= 0) { 
            cout << "Помилка. Сума поповнення має бути > 0." << endl; 
            return; 
        }
        balance += amount; // Лише тут змінюємо приватну змінну
        cout << owner << " поповнив на: $" << amount << endl;
    }

    // Ще один логічний метод (Зняття)
    bool withdraw(double amount) {
        // Захист від балансу в мінус!
        // Гроші за стіною \`private\` у 100% безпеці.
        if (amount > balance) { 
            cout << "Недостатньо коштів для " << owner << "!" << endl; 
            triggerAlarm(); // Виклик приватної функції!
            return false; 
        }
        balance -= amount;
        cout << owner << " зняв: $" << amount << " | Залишок: $" << balance << endl;
        return true;
    }
};

int main() {
    BankAccount acc("Olena", 5000);
    
    // ПОМИЛКА! Не можна ззовні змінити:
    // acc.balance = 999999; // Компілятор видасть ERROR: 'balance' is a private member
    
    // АЛЕ можна безпечно взаємодіяти через інтерфейс:
    cout << "Баланс рахунку: " << acc.getBalance() << endl; // Читання
    
    acc.deposit(1000);
    acc.withdraw(8000); // 8000 > 6000, спрацює захист та тривога!
    acc.withdraw(500);  // Це пройде
    
    return 0;
}`,
        codeExplanation: ["`private:` приховує `owner` та `balance` від об'єкта `acc` у `main()`. Звернутися до них через `acc.owner` просто нереально.", "Всі взаємодії здійснюються через публічну консоль команд: `deposit()` та `withdraw()`.", "У методі `withdraw()` є логіка `if (amount > balance)`. Вона блокує будь-яке пограбування або баги, зберігаючи баланс завжди правильним (Інваріант класу)."],
        practiceTask: 'Додайте до класу Account приватну стрічку `password` та метод `bool changePassword(string oldP, string newP)`. Якщо oldP збігається, змінити на новий і повернути true.',
        expectedOutput: 'Пароль змінено',
        hint: 'У класі: `private: string password = "123";` і метод з `if (oldP == password) { password = newP; return true; }`',
    },

    {
        id: 'oop_static_this',
        title: 'this, Static та Константні методи',
        level: 'oop',
        xp: 45,
        levelLabel: 'ООП (Середній)',
        icon: '📍',
        videos: [],
        explanation: `<h3>1. Що таке вказівник this?</h3>
<p>Іноді в методі класу виникає плутанина. У вас є приватне поле <code>name</code> і в Setter ви подали параметр <code>name</code>:</p>
<code>void setName(string name) { name = name; }</code><br>
<p>Це не працює! Компілятор подумає, що ви присвоюєте локальній змінній саму себе, а поле класу залишається без змін. Треба вказати, що ПЕРШЕ \`name\` - це поле саме ЦЬОГО об'єкта. Для цього є вказівник <strong><code>this-&gt;</code></strong>.</p>
<code>this->name = name;</code>
<p>Він завжди невидимо передається в кожен ваш метод і вказує на об'єкт, який цей метод викликав (якщо викликав \`s1\`, \`this\` вказує на \`s1\`).</p>

<h3>2. Static (Статика): Колективна власність</h3>
<p>Кожен студент має свої власні \`name\`, \`age\`. Але що якщо нам потрібно мати <strong>СПІЛЬНУ ЗМІННУ ДЛЯ ВСІХ СТУДЕНТІВ</strong> одразу? Наприклад кількість створених студентів всього (<code>totalCount</code>). Якщо ми вставимо \`int totalCount\` в об'єкт, у кожного студента буде свій "лічильник".</p>
<p>Тут на допомогу приходить <strong><code>static</code></strong>.</p>
<ul>
  <li>Статичні поля НЕ належать жодному об'єкту! Вони пам'ятаються <strong>в самому кресленні (Класі)</strong> як колективний розум.</li>
  <li>Вона єдина на всю гру / програму. Якщо \`Student s1\` щось туди запише, то \`Student s2\` це побачить!</li>
  <li>Відповідно, для читання цієї змінної є <strong>статичні методи</strong> \`{ static int getCount() { return totalCount; } }\`. Їх викликають не через об'єкт, а через назву класу: \`Student::getCount()\`.</li>
</ul>

<h3>3. Константні методи (const)</h3>
<p>Як сказати іншим програмістам: "Мій Геттер <code>getName()</code> на 100% безпечний, він не змінюватиме дані вашого об'єкта, обіцяю!"?</p>
<p>Додайте слово <code>const</code> до методу в кінці: <code>string getName() const { return name; }</code>. Якщо ви всередині (випадково) спробуєте написати \`name = "hack"\`, компілятор вдарить по руках помилкою!</p>`,
        code: `#include <iostream>
#include <string>
using namespace std;

class User {
private:
    string username;
    
    // СТАТИЧНЕ поле. Лічильник всього класу!
    // Воно ОДНЕ-ЄДИНЕ на всіх. Воно існує навіть коли не створено жодного користувача!
    static int globalUsersCount; 

public:
    // Конструктор
    User(string username) {
        // Вирішуємо конфлікт імен через this->
        this->username = username;
        // Збільшуємо загальний лічильник всього класу
        globalUsersCount++;
    }

    // Деструктор
    ~User() {
        globalUsersCount--;
    }

    // 1. КОНСТАНТНИЙ метод. Гарантує, що не змінить стан об'єкта
    string getUsername() const {
        // username = "error"; // ОШИБКА КОМПІЛЯЦІЇ (Через const)
        return this->username;
    }

    // 2. СТАТИЧНИЙ метод. Не має this! Працює лише із static змінними.
    static int getLobbyCount() {
        return globalUsersCount;
    }
};

// ⚠️ Обов'язкова ініціалізація СТАТИЧНИХ ПОЛІВ зовні класу. 
int User::globalUsersCount = 0;

int main() {
    cout << "Юзерів зараз: " << User::getLobbyCount() << endl; // Можна викликати без об'єкта!

    User* u1 = new User("AlphaGamer");
    User u2("BetaScout");
    User u3("DeltaForce");

    cout << "Знайомтесь: " << u1->getUsername() << endl;
    cout << "Після реєстрації 3 людей Юзерів зараз: " << User::getLobbyCount() << endl;
    
    cout << "\n[Alpha вийшов з гри]" << endl;
    delete u1; // Знищуємо юзера 1

    // У класі 1 лічильник на всіх. Відобразить 2.
    cout << "Юзерів після виходу: " << User::getLobbyCount() << endl;

    return 0;
}`,
        codeExplanation: ["`this->username` означає: 'Поле username мого поточного об'єкта'. Праве `username` - це тимчасовий аргумент.", "`static int globalUsersCount;` — ця змінна схована в класі, вона ділиться між всіма об'єктами. Одна зміна бачиться всіма.", "`User::globalUsersCount = 0;` — глобально пам'ять статичної змінної має бути ініціалізована за межами класу `Class::field = 0;` (Обов'язкове правило C++).", "`const` методики додаються для забезпечення стабільності: ми не хочемо, щоб метод простого отримання імені щось там зламав випадково."],
        practiceTask: 'Немає простої практичної задачі на static, але запам\'ятайте: static і this - потужні інструменти розробника ігор.',
        expectedOutput: 'Юзерів зараз: 1',
        hint: 'Змінюйте статичне поле через Конструктори та Деструктори.',
    },

    {
        id: 'oop_composition',
        title: 'Композиція (Has-A)',
        level: 'oop',
        xp: 50,
        levelLabel: 'ООП (Середній)',
        icon: '🧩',
        videos: [],
        explanation: `<h3>1. Що таке Композиція? (Об'єкти в об'єктах)</h3>
<p>В житті речі складаються з інших речей. Автомобіль (Car) МАЄ двигун (Engine). Геймер (Player) МАЄ зброю (Weapon). В об'єктно-орієнтованому програмуванні це називається відношенням <strong>Has-A (Має)</strong>.</p>
<p>Замість того, щоб в класі гравця прописувати "збройний урон", "збройна перезарядка", "назва зброї", ми створюємо <strong>ОКРЕМИЙ КЛАС Weapon</strong>, і просто вставляємо його як приватне поле у Гравця! Вау!</p>

<h3>2. Як ініціалізувати об'єкт всередині об'єкта?</h3>
<p>Коли ви будуєте Гравця, ви мусите запустити конструктор Зброї, інакше зброя залишиться "сміттям". Це жорстко робиться у списку ініціалізації Гравця через двокрапку.</p>
<code>
class Character {<br>
private:<br>
&nbsp;&nbsp;&nbsp;&nbsp;string name;<br>
&nbsp;&nbsp;&nbsp;&nbsp;Weapon sword; // Використовуємо наш інший клас тут!<br>
public:<br>
&nbsp;&nbsp;&nbsp;&nbsp;Character(string n, string wName, int wDamage)<br>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;: name(n), sword(wName, wDamage) { } // Викликаємо конструктор зброї<br>
}
</code>
<p>Цей підхід робить код максимально гнучким. Клас Зброї можна буде дати і монстрам, і босам, і торговцям (Не повторюючи код!).</p>`,
        code: `#include <iostream>
#include <string>
using namespace std;

// 1. Маленький незалежний клас. 
// Ним можна користуватись будь-де.
class Engine {
private:
    int horsepower;
    string type;
public:
    Engine(int hp, string t) : horsepower(hp), type(t) {
        // Двигун створено.
    }
    
    void start() const {
        cout << "[Двигун] ВРРРУМММ! " << horsepower << " конячих сил!" << endl;
    }
    
    // Геттер
    string getType() const { return type; }
};

// 2. ВЕЛИКИЙ КЛАС.
// Він буде МІСТИТИ у собі об'єкт Engine через композицію.
class Car {
private:
    string brand;
    Engine motor; // Композиція (Car HAS A Engine)
    
public:
    // Конструктор приймає дані для самого себе, та дані ДЛЯ СВОГО ДВИГУНА.
    // Ми ПЕРЕДАЄМО параметри мотора у \`motor(...)\` в стрічці ініціалізації.
    Car(string b, int engHp, string engName)
        : brand(b), motor(engHp, engName) 
    {
    }

    void drive() {
        cout << "\nВодій повертає ключ в своєму " << brand << "..." << endl;
        // Автомобіль делегує роботу своєму двигуну!
        motor.start();
        cout << "Тип вашого агрегату: " << motor.getType() << endl;
        cout << "Ми успішно поїхали." << endl;
    }
};

int main() {
    // Я створюю авто, але в ньому автоматично створюється справжній об'єкт Двигуна!
    Car myCar("BMW M5", 600, "TwinTurbo V8");
    myCar.drive();
    
    Car ecoCar("Nissan Leaf", 147, "Electric EV");
    ecoCar.drive();

    return 0;
}`,
        codeExplanation: ["Класи `Engine` та `Car` існують роздільно, але `Car` поглинає у себе об'єкт `Engine`.", "Зверніть увагу: ми маємо публікувати методи делегації `ecoCar.drive()` у машині, яка всередині запускає приватний `motor.start()`. Таким чином `motor` машини залишається захищеним і неприступним зовні!"],
        practiceTask: 'Напишіть клас `CPU` та клас `Computer` який його включає. Делегуйте метод `process(int data)`.',
        expectedOutput: '[CPU] Processing data',
        hint: 'Аналогічно Car та Engine.',
    },

    {
        id: 'inheritance',
        title: 'Успадкування',
        level: 'oop',
        xp: 50,
        levelLabel: 'ООП (Просунутий)',
        icon: '🧬',
        videos: [{ "id": "5fIuUPUG4jI", "title": "ООП (частина 4). Успадкування" }, { "id": "ijGbhqleWFU", "title": "Успадкування та поліморфізм в C++" }],
        explanation: `<h3>1. Що таке Успадкування? (Спадкоємці)</h3>
<p>Уявіть, що ви створюєте гру. У вас є класи <code>Warrior</code>, <code>Mage</code> та <code>Archer</code>. Всі вони мають <code>hp</code>, <code>name</code>, методи <code>move()</code> та <code>die()</code>. Писати ці однакові змінні 3 рази — порушення правила DRY (Don't Repeat Yourself).<br>
Що якщо створити один <strong>БАЗОВИЙ КЛАС (Батько)</strong> <code>Character</code> з усією базовою логікою, а Маг і Воїн просто <strong>УСПАДКУЮТЬ</strong> його? Вони автоматично отримають всі ці методи та поля абсолютно безкоштовно!</p>

<h3>2. Синтаксис успадкування</h3>
<p>Це робиться за допомогою двокрапки після назви нового класу:</p>
<code>
class Mage : public Character {<br>
&nbsp;&nbsp;&nbsp;&nbsp;int mana; // Додали щось своє, чого немає у звичайного персонажа<br>
};
</code>
<p>Відтепер об'єкт Мага має <code>hp</code> та <code>name</code> (бо він успадкував їх) І <code>mana</code> (своє власне).</p>

<h3>3. Protected (Захищений доступ)</h3>
<p>Якщо ви зробите \`hp\` приватною (<code>private</code>) в <code>Character</code>, то навіть його Діти (Mage) не зможуть до неї напряму звернутися! Приватне означає приватне ТІЛЬКИ для себе.<br>
Щоб дозволити Дітям бачити змінну, але приховати її від <code>main()</code>, використовується специфікатор <strong><code>protected</code></strong>.</p>

<h3>4. Виклик конструктора Батька (Super)</h3>
<p>Коли народжується Маг (Дитина), система спершу <strong>ЗОБОВ'ЯЗАНА</strong> народити його Базис (Батька). Якщо в Батька є строгий конструктор з параметрами (наприклад, ім'я), Маг мусить передати ці параметри в конструктор Батька за допомогою списку ініціалізації!</p>
<code>
Mage(string n, int m) : Character(n), mana(m) {} 
</code>
<div class="note-box">
  💡 <strong>Порядок створення/знищення:</strong> При створенні об'єкта-сина спочатку відпрацьовує Конструктор Батька (фундамент), а потім Сина (дах). А знищуються вони навпаки — спочатку згорить дах (Деструктор Сина), потім фундамент (Деструктор Батька).
</div>`,
        code: `#include <iostream>
#include <string>
using namespace std;

// 1. БАЗОВИЙ КЛАС
class Animal {
protected: // Замінили private на protected, щоб Собака та Кіт мали доступ
    string name;
    int age;

public:
    Animal(string n, int a) : name(n), age(a) { }

    void sleep() {
        cout << name << " зараз міцно спить... Zzz" << endl;
    }
};


// 2. КЛАС СПАДКОЄМЕЦЬ (Dog успадковує ВСЕ у Animal)
class Dog : public Animal {
private:
    string breed; // Унікальне поле собаки (тварина абстрактна породи не має)
    
public:
    // Конструктор Собаки ПЕПЕРЕДАЄ ім'я та вік до конструктора Animal
    Dog(string n, int a, string b) : Animal(n, a), breed(b) { }

    void bark() {
        // Доступ до name можливий, бо воно protected в батькові!
        cout << "[" << breed << "] " << name << " каже: Гав-Гав!" << endl;
    }
};


// 3. ЩЕ ОДИН СПАДКОЄМЕЦЬ (Кішка) 
class Cat : public Animal {
public:
    Cat(string n, int a) : Animal(n, a) {}

    void meow() {
        cout << name << " каже: Мяу! І просить їсти." << endl;
    }
};


int main() {
    Dog rex("Рекс", 4, "Вівчарка");
    Cat murka("Мурка", 2);

    // Ми можемо викликати sleep(), бо Dog і Cat його успішно УСПАДКУВАЛИ
    cout << "\n==== Вечір ====" << endl;
    rex.sleep(); 
    murka.sleep();

    // АЛЕ унікальні методи існують тільки для конкретних класів
    cout << "\n==== Ранок ====" << endl;
    rex.bark(); 
    murka.meow();

    return 0;
}`,
        codeExplanation: ["`class Dog : public Animal` вказує компілятору скопіювати всю публічну і захищену логіку `Animal` в клас `Dog`.", "`Animal(n, a)` всередині списку ініціалізації `Dog` гарантує, що базова частина буде сконструйована правильно.", "Якщо ми спробуємо в main() написати `murka.name = \"Tom\"`, буде помилка! `protected` захищає змінну від зовнішнього втручання, як і `private`."],
        practiceTask: 'Напишіть базовий клас \'Vehicle\' з полем \'speed\' і методом move(). Від нього успадкуйте \'Bicycle\' з методом ringBell().',
        expectedOutput: 'Vehicle is moving at 20 kmh\nRing-ring!',
        hint: 'class Bicycle : public Vehicle { public: Bicycle(int s) : Vehicle(s) {} void ringBell() { cout << "Ring"; } };',
    },

    {
        id: 'polymorphism',
        title: 'Поліморфізм та Перевизначення',
        level: 'oop',
        xp: 50,
        levelLabel: 'ООП (Просунутий)',
        icon: '🎭',
        videos: [{ "id": "3m_-pT6uBWs", "title": "#55 Поліморфізм та Віртуальні функції" }],
        explanation: `<h3>1. Що таке Поліморфізм? (Багато Форм)</h3>
<p>Слово "Поліморфізм" з грецької перекладається як "багато форм". Це здатність викликати ОДИН І ТОЙ ЖЕ метод на різних об'єктах і отримувати РІЗНИЙ результат. 
Наприклад, команда <code>speak()</code>. Якщо викликати її на об'єкті Собака, отримаємо "Гав", на Кіт — "Мяу", Качка — "Кря".</p>

<h3>2. Перевизначення (Override)</h3>
<p>Коли ви успадкували метод \`speak()\` від Animal, він робить щось стандартне (напр. мовчить). Але ви хочете змінити цю поведінку спеціально для Собаки. В C++ це робиться через перевизначення.</p>

<h3>3. Ключові слова <code>virtual</code> та <code>override</code></h3>
<div class="note-box">
  🔥 <strong>Увага:</strong> В C++ функції Батька жорстко зафіксовані (статичне зв'язування) для швидкості. Якщо код "думає", що це Animal, він викличе \`Animal::speak()\`, навіть якщо під капотом це насправді Dog!
</div>
<p>Щоб компілятор у процесі роботи (динамічне зв'язування) запитав себе: <em>"А хто це НАСПРАВДІ? Собака? Тоді запущу Собачий метод!"</em>, ви <strong>ЗОБОВ'ЯЗАНІ</strong> помітити метод Батька словом <code>virtual</code>.</p>
<ul>
  <li><code>virtual void speak()</code> — (В базовому класі). Каже: "Мої діти можуть мене замінити. Завжди перевіряй перед викликом, чий це насправді об'єкт."</li>
  <li><code>void speak() override</code> — (В класі сина). Слово \`override\` це запобіжник від описок (C++11). Воно скаже вам, якщо ви помилилися в назві методу під час заміни.</li>
</ul>

<h3>4. Абстрактні класи (Інтерфейси)</h3>
<p>Іноді Батьківський клас є настільки загальним (як <code>Shape</code> - Фігура), що він не може самостійно відповісти на питання "Яка в мене площа?". Тоді створюються <strong>Чисто віртуальні функції</strong>, які закінчуються на <code>= 0;</code>.</p>
<code>virtual double getArea() = 0;</code>
<p>Клас з такою функцією є <strong>Абстрактним</strong>. Це означає, що ви <strong>НЕ МОЖЕТЕ</strong> створити об'єкт \`Shape s;\`! Будь-яка дитина цього класу (Circle, Rectangle) <strong>ЗОБОВ'ЯЗАНА</strong> написати свій код площі, інакше теж стане абстрактною!</p>`,
        code: `#include <iostream>
#include <string>
#include <vector>
using namespace std;


// 1. АБСТРАКТНИЙ базовий клас (Інтерфейс).
class Enemy {
protected:
    int health;
public:
    Enemy(int hp) : health(hp) {}
    
    // ВІРТУАЛЬНИЙ МЕТОД: Дозволяє динамічне заміщення
    virtual void attack() {
        cout << "[Базовий Ворог] б'є слабо." << endl;
    }
    
    // ЧИСТО ВІРТУАЛЬНИЙ МЕТОД: Робить клас Абстрактним! (Не можна створити просто Enemy)
    virtual void showDeath() = 0;
    
    virtual ~Enemy() {} // ДУЖЕ ВАЖЛИВО! Деструктор завжди має бути virtual
};


// 2. СПАДКОЄМЕЦЬ Гоблін
class Goblin : public Enemy {
public:
    Goblin(int hp) : Enemy(hp) {}
    
    // Перевизначаємо метод атаки
    void attack() override {
        cout << "[Гоблін] штрикає вас малим кинджалом! (-10 hp)" << endl;
    }
    
    void showDeath() override {
        cout << "Гоблін із криком падає..." << endl;
    }
};


// 3. СПАДКОЄМЕЦЬ Орк (Броньований Бос)
class Orc : public Enemy {
public:
    Orc(int hp) : Enemy(hp) {}
    
    void attack() override {
        cout << "[Орк] розмахує важкою СОКИРОЮ! (-50 hp)" << endl;
    }
    
    void showDeath() override {
        cout << "Орк падає на коліна з глухим гуркотом." << endl;
    }
};


int main() {
    // Enemy e(100);  // ПОМИЛКА! Enemy тепер абстрактний (= 0).

    cout << "=== Масив Ворогів (Покажчики Батька) ===" << endl;
    // Крута фішка ООП: Масив батьківських покажчиків може зберігати БУДЬ-ЯКИХ спадкоємців!
    vector<Enemy*> dungeon;
    dungeon.push_back(new Goblin(100)); // Закидаємо Гобліна
    dungeon.push_back(new Orc(500));    // Закидаємо Орка

    // Ми не знаємо хто є хто. Просто кажемо ВСІМ атакувати!
    for (Enemy* mob : dungeon) {
        // Завдяки VIRTUAL, компілятор сам зрозуміє хто це, і викличе ПРАВИЛЬНИЙ метод.
        mob->attack(); 
    }

    // Симулюємо їхню смерть
    cout << "\n==== Перемога Гравця ====" << endl;
    for (Enemy* mob : dungeon) {
        mob->showDeath();
        delete mob; // Правильно звільняємо пам'ять (завдяки virtual ~Enemy)
    }

    return 0;
}`,
        codeExplanation: ["`virtual void attack()` в Батькові дозволяє магію Поліморфізму: хоча масив списку складається з базових `Enemy*`, кожен ворог атакує по-свому.", "`virtual void showDeath() = 0;` — робить метод чисто віртуальним, і змушує всіх хто успадкує Enemy написати свою реалізацію `showDeath`.", "`virtual ~Enemy()` — віртуальний деструктор життєво необхідний для ієрархій! Інакше `delete mob` знищить лише `Enemy` частину і залишить витік пам'яті!"],
        practiceTask: 'Створіть абстрактний батько \'Shape\' з `virtual double area() = 0`. Від нього створіть `Square(int side)`. В main() виведіть площу квадрата.',
        expectedOutput: '25',
        hint: 'Square : public Shape. Метод: `double area() override { return side*side; }`',
    },

    {
        id: 'operator_overloading',
        title: 'Перевантаження операторів (+, ==)',
        level: 'oop',
        xp: 50,
        levelLabel: 'ООП (Гуру)',
        icon: '➕',
        videos: [{ "id": "3yYFear-5HA", "title": "Урок 19 ⦁ Перевантаження операторів" }, { "id": "SkxOfIdkKrg", "title": "#44 Перевантаження операторів" }],
        explanation: `<h3>1. Що таке Перевантаження операторів?</h3>
<p>В C++ ви можете працювати з об'єктами так само легко, як із числами!
Коли ви пишете <code>5 + 3</code>, компілятор знає що робити. Але якщо ви створите клас <code>Vector2(x, y)</code> і спробуєте <code>v1 + v2</code>, компілятор скаже: <em>"Я не знаю як додавати Вектори!"</em>.</p>
<p>Перевантаження вчить ваші класи розуміти оператори <code>+</code>, <code>-</code>, <code>&gt;</code>, <code>==</code>, <code>++</code> тощо.</p>

<h3>2. Синтаксис перевантаження</h3>
<p>Спеціальне ключове слово <code>operator</code>. Фактично це звичайна функція, яка повертає новий об'єкт.</p>
<code>
Vector2 operator+(const Vector2& other) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Логіка: мій \`x\` додати інший \`other.x\`<br>
&nbsp;&nbsp;&nbsp;&nbsp;return Vector2(x + other.x, y + other.y);<br>
}
</code>
<div class="note-box">
  💡 <strong>Що таке \`const Vector2& other\`?</strong> Це передача за посиланням (щоб не копіювати весь важкий об'єкт) із забороною зміни (\`const\`). Це еталон стилю в C++.
</div>

<h3>3. Оператор порівняння (==)</h3>
<p>Ви можете перевантажити \`==\`, щоб навчити програму перевіряти, чи однакові два предмети в інвентарі (якщо в них збігаються ID наприклад, або Характеристики). Він повертає <code>bool</code>.</p>
<code>
bool operator==(const Item& i) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;return this->price == i.price;<br>
}
</code>`,
        code: `#include <iostream>
using namespace std;

class Vector2 {
public:
    double x;
    double y;

    // Конструктор
    Vector2(double x_val = 0, double y_val = 0) : x(x_val), y(y_val) {}

    // --- ПЕРЕВАНТАЖЕННЯ ОПЕРАТОРА ДОДАВАННЯ (+) ---
    // Метод приймає ПРАВУ частину (b) у виразі (a + b)
    Vector2 operator+(const Vector2& rightsS) const {
        return Vector2(this->x + rightsS.x, this->y + rightsS.y);
    }
    
    // --- ПЕРЕВАНТАЖЕННЯ ВІДНІМАННЯ (-) ---
    Vector2 operator-(const Vector2& r) const {
        return Vector2(this->x - r.x, this->y - r.y);
    }

    // --- ПЕРЕВАНТАЖЕННЯ ПОРІВНЯННЯ (==) ---
    bool operator==(const Vector2& r) const {
        // Вважаємо їх однаковими, якщо координати повністю ідентичні
        if (this->x == r.x && this->y == r.y) return true;
        return false;
    }

    void show() const {
        cout << "(" << x << "; " << y << ")" << endl;
    }
};

int main() {
    Vector2 pos1(10, 20); // Поточна позиція гравця
    Vector2 move(5, -2);  // Вектор руху (швидкість)

    cout << "Старт гравця: "; pos1.show();
    cout << "Дія: додаємо рух + (5; -2)." << endl;

    // 🏆 МАГІЯ! Додаємо два об'єкти мовби це прості числа!
    // Під капотом це викликає pos1.operator+(move)
    Vector2 result = pos1 + move; 

    cout << "Нова позиція: "; result.show();

    // Порівняння ==
    Vector2 vA(5, 5);
    Vector2 vB(5, 5);
    Vector2 vC(0, 0);

    if (vA == vB) cout << "vA та vB ідентичні!" << endl;
    if (!(vA == vC)) cout << "vA та vC різні!" << endl;

    return 0;
}`,
        codeExplanation: ["`Vector2 operator+(const Vector2& rightsS) const` — це декларація. `Vector2` (перше слово) вказує, що оператор породить НОВИЙ вектор як результат.", "`this->x + rightsS.x` бере локальний (лівий) X та додає йому правий переданий X. Робить те саме з Y і конструює новий об'єкт `Vector2(x, y)` на повернення.", "З операторами код стає в 10 разів більш читабельним у фізичних та ігрових рушіях."],
        practiceTask: 'Навчіть клас \'Money\' розуміти оператор +, щоб Money(100) + Money(50) повертало Money(150), і виведіть результат.',
        expectedOutput: '150',
        hint: 'У тілі Money: `Money operator+(const Money& o) { return Money(money + o.money); }`',
    },

    {
        id: 'oop_operator_io',
        title: 'Перевантаження Вводу/Виводу (<<, >>) (Friend)',
        level: 'oop',
        xp: 55,
        levelLabel: 'ООП (Гуру)',
        icon: '🗣️',
        videos: [],
        explanation: `<h3>1. Проблема виводу Об'єктів</h3>
<p>У попередньому уроці нам доводилося писати метод \`show()\` для виводу координат на екран: <code>pos.show();</code>. Але набагато зручніше було б писати просто <code>cout &lt;&lt; pos;</code>, як із звичайними числами! Якщо ви спробуєте зробити це без перевантаження, компілятор видасть страшну 200-рядкову помилку.</p>

<h3>2. Чому оператор << особливий?</h3>
<p>Коли ви пишете \`a + b\`, цей оператор належить об'єкту \`a\`. Але коли ви пишете \`cout &lt;&lt; a\`, ліворуч стоїть не ваш об'єкт \`a\`, а системний об'єкт \`cout\` (типу <code>ostream</code>)! Ви НЕ МОЖЕТЕ залізти в системний клас <code>ostream</code> і дописати туди новий метод для вашого класу.</p>
<p>Рішення: перевантажити оператор <strong>ГЛОБАЛЬНО</strong> (не всередині класу). Але глобальні функції не бачать \`private\` поля... Тут і з'являється слово <code>friend</code> (друг).</p>

<h3>3. Функції-Друзі (friend)</h3>
<p>Функція-друг — це функція, яка визначається <strong>ПОЗА</strong> межами класу, але клас дає їй VIP-путівку на доступ до своїх <code>private</code> полів!</p>
<code>
class Character {<br>
private: int hp = 100;<br>
&nbsp;&nbsp;&nbsp;&nbsp;// Даємо дозвіл дружній глобальній функції<br>
&nbsp;&nbsp;&nbsp;&nbsp;friend ostream& operator<<(ostream& os, const Character& obj);<br>
};
</code>
<p>Функція <code>operator&lt;&lt;</code> повертає посилання <code>ostream&</code> (той самий \`cout\`), щоб ви могли будувати "ланцюжок" виводу: <code>cout &lt;&lt; a &lt;&lt; " txt";</code>.</p>`,
        code: `#include <iostream>
#include <string>
using namespace std;

class Fraction {
private:
    int numerator;   // чисельник
    int denominator; // знаменник

public:
    Fraction(int num = 0, int den = 1) : numerator(num), denominator(den) {}

    // 1. Оголошуємо оператор << ДРУГОМ класу
    // Це НЕ метод класу. Ми просто кажемо: "Ось ця зовнішня функція має доступ до мого private!"
    friend ostream& operator<<(ostream& out, const Fraction& frac);
    
    // 2. Оголошуємо оператор >> ДРУГОМ для читання з клавіатури через cin
    friend istream& operator>>(istream& in, Fraction& frac);
};

// === ГЛОБАЛЬНІ ФУНКЦІЇ ЗОВНІ КЛАСУ ===

// Оператор << виводить дамп нашого класу у переданий потік (out)
ostream& operator<<(ostream& out, const Fraction& frac) {
    // Дякуючи VIP-friend доступу, можемо спокійно читати приватні frac.numerator!
    out << frac.numerator << "/" << frac.denominator;
    // Обов'язково повертати об'єкт потоку назад для ланцюгового виклику (cout << a << b;)
    return out;
}

// Оператор >> завантажує дані З потоку в об'єкт (передача ЗМІНЮВАНОГО посилання, без const!)
istream& operator>>(istream& in, Fraction& frac) {
    char slash; // Фіктивна змінна для зчитування розділового знаку (слеша)
    in >> frac.numerator >> slash >> frac.denominator;
    return in;
}

int main() {
    Fraction f1(3, 4);
    
    // Магія в дії! Виводить дроби як рідні об'єкти!
    cout << "Мій перший дріб: " << f1 << endl;
    
    Fraction f2;
    cout << "Введіть дріб у форматі (Число/Число), наприклад 5/8: ";
    cin >> f2; // Працює бездоганно!
    
    cout << "Ви ввели: " << f2 << endl;
    cout << "Кінець програми." << endl;

    return 0;
}`,
        codeExplanation: ["`friend` пишеться ВИКЛЮЧНО в тілі класу. Це сертифікат довіри до зовнішньої функції.", "`ostream&` — це клас `cout`. Оператор приймає поточний потік виводу ліворуч, і об'єкт класу праворуч.", "Об'єкт потоку `out` повертається за посиланням `return out`, що забезпечує безперервний виклик операторів."],
        practiceTask: 'Зробіть для \'Player\' (з private name і score) friend оператор `<<`, який виведе \'Гравець [ім\'я]: [очки] xp\'.',
        expectedOutput: 'Гравець Ivan: 100 xp',
        hint: 'friend ostream& operator<<(ostream& os, const Player& p) { os << "Гравець " << p.name << ": " << p.score << " xp"; return os; }',
    },

    {
        id: 'templates_exceptions',
        title: 'Шаблони та Винятки (Exceptions)',
        level: 'oop',
        xp: 60,
        levelLabel: 'ООП (Експерт)',
        icon: '🔥',
        videos: [],
        explanation: `<h3>1. Що таке Шаблони (Templates)? Дженерики</h3>
<p>Ви написали геніальну функцію <code>swap(int& a, int& b)</code>. Але раптом знадобилося міняти місцями \`double\`. Потім \`string\`. Писати 3 однакові функції? НІ! <strong>Шаблони (Templates)</strong> дозволяють створити Універсальну Функцію (або Клас), де тип даних — це параметр <code>T</code>.</p>
<code>
template &lt;typename T&gt;<br>
void mySwap(T& a, T& b) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;T temp = a;<br>
&nbsp;&nbsp;&nbsp;&nbsp;a = b; b = temp;<br>
}
</code>
<p>Під час компіляції система сама створить правильні варіанти для \`int\`, \`double\` etc. (Це називається Метапрограмування).</p>

<h3>2. Обробка виняткових ситуацій (try / catch)</h3>
<p>Іноді в програмі трапляється трагедія, яка не залежить від коду (наприклад: зник інтернет під час завантаження гри, або користувач ввів Ділення на НУЛЬ). Замість того, щоб програма "крашнулась" з фатальною помилкою, C++ дозволяє "перехоплювати" такі помилки.</p>
<ul>
  <li><code>throw "Помилка!";</code> — Оператор, який "кидає гранату". Програма негайно зупиняє лінійне виконання і шукає, хто "впіймає" її.</li>
  <li><code>try { ... }</code> — Зона ризику (Щит). Блок коду, де ви розраховуєте на можливість помилки.</li>
  <li><code>catch (...) { ... }</code> — Зона порятунку (Сапер). Блок коду, який миттєво активується, якщо в \`try\` був <code>throw</code>. Він обробляє гранату без падіння програми.</li>
</ul>

<h3>3. Стандартні винятки (<stdexcept>)</h3>
<p>C++ має збірку готових класів "гранат" для частих ситуацій: <code>invalid_argument</code>, <code>out_of_range</code>, або базовий <code>exception</code>.</p>`,
        code: `#include <iostream>
#include <string>
#include <stdexcept> // Бібліотека стандартних винятків
using namespace std;

// === 1. ШАБЛОННИЙ КЛАС ===
// Дозволяємо класу бути "Гумовим" - підлаштовуватись під T
template <typename T>
class Storage {
private:
    T item;
public:
    Storage(T i) : item(i) {}
    T getItem() const { return item; }
};

// Функція з вбудованим АЛАРМОМ
double safeDivide(double a, double b) {
    if (b == 0) {
        // Ми "генеруємо" об'єкт винятку. 
        // Виконання ФУНКЦІЇ МИТТЄВО ПЕРЕРИВАЄТЬСЯ НА ЦЬОМУ РЯДКУ!
        throw runtime_error("Критична помилка: Ділення на НУЛЬ!");
    }
    return a / b;
}

int main() {
    // Демонстрація шаблонів (зверніть увагу на кутові дужки <>)
    Storage<int> intBox(42);
    Storage<string> strBox("Ключ від сейфу");
    
    cout << "В коробці: " << intBox.getItem() << endl;
    cout << "В іншій: " << strBox.getItem() << endl;

    // === 2. РОБОТА З ВИЩАТКАМИ (TRY - CATCH) ===
    cout << "\n==== Запуск безпеки ====" << endl;
    
    try {
        cout << "Спроба 1: " << safeDivide(10, 2) << endl;
        
        cout << "Спроба 2: РИЗИК." << endl;
        // Тут буде генерований виняток! Увесь код після цього рядка всередині TRY - відкидається.
        double result = safeDivide(50, 0); 
        
        cout << "Це виведеться тільки якщо все добре: " << result << endl;
    } 
    catch (const exception& error) {
        // Ми успішно перехопили помилку! Програма НЕ падає.
        cout << "\n[CATCH_BLOCK ВІДПРАЦЮВАВ] Ми запобігли крашу додатку!" << endl;
        // виводимо .what() - текст помилки
        cout << "Причина: " << error.what() << endl; 
    }

    cout << "\nГра продовжує стабільно працювати." << endl;

    return 0;
}`,
        codeExplanation: ["`template <typename T>` — `T` грає роль змінної для ТИПУ ДАНИХ. Клас або функція може працювати з нею так само, як з int.", "Коли ми кидаємо `throw runtime_error(...)`, C++ шукає найближчий блок `catch`. Якщо його не знайде — програма завершується аварійно.", "`error.what()` в блоці `catch` містить текстове пояснення причини помилки, яку ми вказали."],
        practiceTask: 'Напишіть `try { throw runtime_error("Fail!"); } catch (const exception& e) { cout << "Caught: " << e.what(); }`',
        expectedOutput: 'Caught: Fail!',
        hint: 'Просто дослівно відтворіть цей блок.',
    },

];
